---
name: New model / compatibility report
about: Report that your AC model works (or doesn't) with this integration
---

**AC brand and model:**
**App name on the store:**
**App package name** (from `adb shell pm list packages | grep -i <brand>`):
**MQTT broker hostname** (from APK JS source or mitmproxy):
**Does it work with this integration?** Yes / Partially / No

**If partially or no — what's different in the protocol?**
(Paste example MQTT payloads if you captured them)
